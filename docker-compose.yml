services:
  db:
    image: postgres:16
    container_name: futurisys-postgres
    environment:
      POSTGRES_USER: futurisys
      POSTGRES_PASSWORD: futurisys
      POSTGRES_DB: futurisys
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
